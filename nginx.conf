# server {
#     # 監聽 Port 80 (標準 HTTP)
#     listen 80;
    
#     # 這裡填你的網域或 IP，還沒買網域先用底線 _ 代表「接收所有請求」
#     server_name _;

#     # 1. 前端設定 (Next.js)
#     # 當使用者連到首頁 (/) 時
#     location / {
#         # 轉發給本機的 Next.js (Port 3000)
#         proxy_pass http://127.0.0.1:3000;
        
#         # 以下是標準設定，確保 WebSocket 和 Header 正常傳遞
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#     }

#     # 2. 後端設定 (FastAPI)
#     # 當使用者連到 /api/ 開頭的網址時
#     location /api/ {
#         # 關鍵！注意網址最後面的斜線 /
#         # 這會把 "/api/" 這段網址剪掉，剩下的傳給後端
#         # 例如：前端呼叫 /api/users -> Nginx 轉發給後端 http://127.0.0.1:8000/users
#         proxy_pass http://127.0.0.1:8000/;
        
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }
server {
    listen 80;
    server_name _;

    # ⚠️ 關鍵設定：因為你的 AI 回覆是 Stream (流式傳輸)
    # 必須把 Nginx 的緩衝關掉，不然它會等整句話收完才吐給前端，就沒有打字機效果了
    proxy_buffering off;
    proxy_read_timeout 300s
    # 1. 前端設定
    location / {
        # ❌ 不能寫 http://127.0.0.1:3000
        # ✅ 要寫 docker-compose.yml 裡面的服務名稱 "frontend"
        proxy_pass http://frontend:3000;
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # 2. 後端設定
    location /api/ {
        # ❌ 不能寫 http://127.0.0.1:8000/
        # ✅ 要寫 docker-compose.yml 裡面的服務名稱 "backend"
        # 注意結尾的斜線 / 依然要保留，用來修剪路徑
        proxy_pass http://backend:8000/;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}